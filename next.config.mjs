/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 's.adroll.com', 's0.2mdn.net', 'periscopeng.com', 'example.com', 'plus.unsplash.com', 'unsplash.com']
    }
};

export default nextConfig;
